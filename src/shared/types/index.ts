// Shared message types between DevTools panel, background service worker, and content script
// Re-exports from the main shared types module
export {
  MessageType,
  type CapturedElement,
  type ElementState,
  type ElementHierarchy,
  type ExtensionMessage,
} from '../types';
